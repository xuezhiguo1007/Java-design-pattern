单例模式是属于创建型模式的一种(另外两种模式是结构型模式，行为型模式），是设计模式最简单的一种。
从数学含义讲是：有且仅有一个元素的集合
从实现的层面看，又类自身管理自己的唯一对象，这个类提供了方法来访问该对象的方式，可以直接访问，不能实例化。
确保某个类只有一个实例，而且自行实例化并向整个系统创建这个实例
三个要点：一是某个类只能有一个实例：二是必须自行创建这个实例，三是必须向整个系统提供这个实例

